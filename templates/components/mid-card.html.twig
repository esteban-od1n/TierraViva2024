<div class="rounded-xl flex flex-col p-4 bg-gradient-to-b from-timber-green-900 to-lochinvar-400 gap-4">
    <h3 class="font-label text-pistachio-400 text-2xl font-bold text-center">{{ text|default("") }}</h3>
    <img src="{{ img|default('') }}" alt="" class="h-32 object-contain">
</div>
