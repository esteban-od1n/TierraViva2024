
<div class="bg-white shadow-lg rounded-lg p-5 max-w-xs">
    <img class="rounded-lg w-full h-48 object-cover" src="{{ image }}" alt="Pera Dulce" />
    <div class="mt-4">
        <div class="text-xl font-bold mb-2">{{title}}</div>
        <div class="text-gray-700">{{description}}</div>
        <div class="text-lg font-bold text-green-500">$ {{price}}</div>
    </div>
</div>
