<a class="rounded-xl flex flex-col p-4 bg-gradient-to-b from-timber-green-900 to-lochinvar-400 gap-2" href="{{ href ?? '' }}">
    <img src="{{ img|default('') }}" alt="" class="h-20 object-contain">
    <span class="font-label text-white text-center">{{ text|default("") }}</span>
</a>
