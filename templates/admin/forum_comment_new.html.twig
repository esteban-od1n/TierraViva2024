{% extends 'admin/base.html.twig' %}

{% block body %}
<div class="flex flex-col py-5 gap-3 m-8">
    <h1 class="font-heading text-6xl">Comentar</h1>
    {{ form_start(form) }}
        {{form_row(form.body)}}
        {{form_row(form.post)}}
        {{form_row(form.author)}}
        {% include 'components/button/default.html.twig' with {text: 'Comentar'} %}
    {{ form_end(form) }}
</div>
{% endblock %}
